#include <bits/stdc++.h>
using ll = long long;
// using namespace std;

void solve() {

}

int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
    std::cout.tie(nullptr);

    int tc = 1;
    std::cin >> tc;

    while(tc--) { solve(); }
    return 0;
}